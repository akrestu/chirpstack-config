services:
  chirpstack:
    image: chirpstack/chirpstack:4
    command: -c /etc/chirpstack
    restart: unless-stopped
    volumes:
      - ./configuration/chirpstack:/etc/chirpstack
      - chirpstack-data:/var/lib/chirpstack
    depends_on:
      - postgres
      - redis
      - mosquitto
    environment:
      - MQTT_BROKER_HOST=mosquitto
      - REDIS_HOST=redis
      - POSTGRESQL_HOST=postgres
      - SERVICE_FQDN_CHIRPSTACK_8080
    labels:
      - "coolify.managed=true"

  chirpstack-gateway-bridge:
    image: chirpstack/chirpstack-gateway-bridge:4
    restart: unless-stopped
    ports:
      - "1700:1700/udp"
    volumes:
      - ./configuration/chirpstack-gateway-bridge:/etc/chirpstack-gateway-bridge
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://mosquitto:1883
    depends_on:
      - mosquitto

  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - type: bind
        source: ./configuration/mosquitto/config/mosquitto.conf
        target: /mosquitto/config/mosquitto.conf
        content: |
          listener 1883
          allow_anonymous true
          persistence true
          persistence_location /mosquitto/data/
          log_dest stdout
      - mosquitto-data:/mosquitto/data

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    volumes:
      - postgresql-data:/var/lib/postgresql/data
      - type: bind
        source: ./configuration/postgresql/initdb/001-chirpstack_extensions.sh
        target: /docker-entrypoint-initdb.d/001-chirpstack_extensions.sh
        content: |
          #!/bin/bash
          set -e

          psql -v ON_ERROR_STOP=1 --username "$$POSTGRES_USER" --dbname="$$POSTGRES_DB" <<-EOSQL
              create extension pg_trgm;
              create extension hstore;
          EOSQL
    environment:
      - POSTGRES_USER=chirpstack
      - POSTGRES_PASSWORD=chirpstack
      - POSTGRES_DB=chirpstack

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis-data:/data

volumes:
  chirpstack-data:
  postgresql-data:
  redis-data:
  mosquitto-data: